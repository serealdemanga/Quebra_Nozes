{
  "name": "esquilo-invest-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --env local",
    "dev:local": "wrangler dev --env local",
    "deploy": "wrangler deploy --env production",
    "deploy:hml": "wrangler deploy --env hml",
    "deploy:prod": "wrangler deploy --env production",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260320.0",
    "typescript": "^5.8.2",
    "wrangler": "^4.8.0"
  }
}
